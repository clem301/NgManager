name: Auto Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: 🔄 Pull latest changes
      run: |
        cd /var/www/ngmanager
        git pull origin main

    - name: 📦 Install dependencies
      run: |
        cd /var/www/ngmanager
        npm install

    - name: 🏗️ Build application
      run: |
        cd /var/www/ngmanager
        npm run build

    - name: 🚀 Restart PM2
      run: |
        cd /var/www/ngmanager
        pm2 restart ngmanager || pm2 start ecosystem.config.js
        echo "✅ Déploiement automatique terminé !"
